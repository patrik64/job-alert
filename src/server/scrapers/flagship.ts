import { greenhouseBoard } from './greenhouse';

// flagshippioneering.com/join/roles is a greenhouse board whose offices are
// the portfolio companies
export const board = greenhouseBoard({ token: 'flagshippioneeringinc' });
