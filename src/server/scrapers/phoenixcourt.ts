import { considerBoard } from './consider';

// the group board of LocalGlobe, Latitude and Solar — consider still knows
// it by its old name
export const board = considerBoard({ host: 'jobs.phoenixcourt.vc', boardId: 'localglobe-all' });
