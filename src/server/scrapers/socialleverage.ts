import { getroBoard } from './getro';

// socialleverage.com/careers is a custom front-end over this hosted board
export const board = getroBoard({ host: 'jobs.socialleverage.com', collectionId: 1371 });
