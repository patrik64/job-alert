import { getroBoard } from './getro';

// springtide (jobs.springtide.com) and springtime
// (careers.springtimeventures.com) are different funds
export const board = getroBoard({ host: 'jobs.springtide.com', collectionId: 3129 });
